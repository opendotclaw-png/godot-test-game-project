# opendotclaw

Starter repo for your OpenClaw workspace.

## What’s here
- Workspace behavior/persona files (`SOUL.md`, `AGENTS.md`, etc.)
- Memory files under `memory/`
- Local helper notes in `TOOLS.md`

## Quick start
```bash
git status
git add .
git commit -m "chore: update workspace"
```
